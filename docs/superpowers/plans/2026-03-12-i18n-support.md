# 国际化 (i18n) 实现方案

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Nano Gallery 添加中英文语言切换功能，支持用户界面完全国际化。

**Architecture:** 采用 next-intl 库实现国际化，通过中间件路由处理语言前缀（/en、/zh），使用 Cookie 存储用户语言偏好。翻译文件按模块组织，组件使用 useTranslations Hook 获取翻译文本。

**Tech Stack:** Next.js 14 App Router, next-intl, Cookie-based locale persistence

---

## 文件结构规划

```
app/
├── [locale]/                    # 动态语言路由
│   ├── layout.tsx              # 语言特定布局
│   └── page.tsx                # 首页
├── actions.ts                  # Server Actions (添加 getLocaleAction)
├── layout.tsx                  # 根布局 (删除，移到 [locale]/)
└── globals.css

components/
├── LanguageSwitcher.tsx        # 新建：语言切换组件
├── GeneratorModal.tsx          # 修改：添加翻译
├── LoginModal.tsx              # 修改：添加翻译
├── TemplateCard.tsx            # 修改：添加翻译
└── ...

i18n/
├── request.ts                  # next-intl 配置
├── routing.ts                  # 路由配置
└── messages/
    ├── en.json                 # 英文翻译
    └── zh.json                 # 中文翻译

middleware.ts                   # 更新：添加语言检测中间件
```

---

## Task 1: 安装依赖并配置 next-intl

**Files:**
- Modify: `package.json`
- Create: `i18n/request.ts`
- Create: `i18n/routing.ts`

- [ ] **Step 1: 安装 next-intl 依赖**

```bash
npm install next-intl
```

- [ ] **Step 2: 创建 i18n 路由配置**

创建 `i18n/routing.ts`:

```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['en', 'zh'],
  defaultLocale: 'zh'
});
```

- [ ] **Step 3: 创建 next-intl 请求配置**

创建 `i18n/request.ts`:

```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale;

  // 确保 locale 有效
  if (!locale || !routing.locales.includes(locale as any)) {
    locale = routing.defaultLocale;
  }

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default
  };
});
```

- [ ] **Step 4: 验证安装成功**

运行: `npm run build`
预期: 构建成功（可能有警告但不报错）

---

## Task 2: 创建翻译文件

**Files:**
- Create: `i18n/messages/en.json`
- Create: `i18n/messages/zh.json`

- [ ] **Step 1: 创建中文翻译文件**

创建 `i18n/messages/zh.json`:

```json
{
  "common": {
    "loading": "加载中...",
    "login": "登录",
    "logout": "退出登录",
    "create": "创建",
    "tryDemo": "试用演示",
    "cancel": "取消",
    "save": "保存",
    "download": "下载",
    "search": "搜索",
    "clear": "清除"
  },
  "header": {
    "title": "Nano Banana",
    "pro": "Pro",
    "subtitle": "Gemini 3 Pro 画廊",
    "credits": "积分",
    "superUser": "超级用户",
    "creator": "创作者"
  },
  "hero": {
    "title": "探索与创造，尽在",
    "titleHighlight": "Nano Banana Pro",
    "description": "探索精选的 Gemini 3 Pro 提示词和模板库。生成惊艳的视觉作品，重新混合现有风格，构建您的个人收藏。"
  },
  "search": {
    "placeholder": "搜索模板标题或提示词..."
  },
  "tabs": {
    "gallery": "画廊",
    "myLibrary": "我的收藏",
    "review": "审核",
    "publicStyles": "公共模板",
    "pendingStyles": "待审核",
    "personalStyles": "个人模板"
  },
  "template": {
    "refImage": "参考图",
    "refs": "张参考图",
    "pending": "待审核",
    "rejected": "已拒绝",
    "approve": "批准",
    "reject": "拒绝",
    "tryThis": "试用此模板",
    "by": "作者",
    "anonymous": "匿名",
    "copyPrompt": "复制提示词"
  },
  "empty": {
    "noTemplates": "未找到模板",
    "libraryEmpty": "您还没有创建任何模板。开始创作，建立您的个人收藏。",
    "galleryEmpty": "公共画廊暂无内容。",
    "createFirst": "创建第一个模板"
  },
  "loadMore": "加载更多",
  "footer": {
    "copyright": "版权所有 © 2025，由 XCC 创建"
  },
  "generator": {
    "title": "工作室",
    "parameters": "参数",
    "preview": "预览",
    "prompt": "提示词",
    "promptPlaceholder": "描述您的想象...",
    "referenceImages": "参考图片",
    "addReferences": "添加参考",
    "enabled": "已启用",
    "addImages": "添加图片",
    "ratio": "比例",
    "quality": "质量",
    "count": "数量",
    "standard": "标准",
    "high": "高清",
    "ultra": "超清",
    "availableCredits": "可用积分",
    "loginToGenerate": "登录以生成",
    "generating": "生成中...",
    "insufficientCredits": "积分不足",
    "generate": "生成",
    "credit": "积分",
    "credits": "积分",
    "publishRemix": "发布混音",
    "saveTemplate": "保存模板",
    "previewPlaceholder": "预览将在此显示",
    "selected": "已选择",
    "editParameters": "编辑参数"
  },
  "save": {
    "title": "保存到画廊",
    "description": "为您的作品起一个难忘的名字。",
    "titlePlaceholder": "例如：霓虹赛博武士",
    "publicTemplate": "公开模板",
    "privateDraft": "私密草稿",
    "publicDesc": "对所有人可见",
    "privateDesc": "仅在您的收藏中可见",
    "saveTemplate": "保存模板"
  },
  "exitConfirm": {
    "title": "未保存的更改",
    "description": "您有未保存的作品。确定要放弃并离开吗？",
    "keepEditing": "继续编辑",
    "discardExit": "放弃并退出"
  },
  "login": {
    "joinStudio": "加入工作室",
    "welcomeBack": "欢迎回来",
    "joinDesc": "创建您的个人资料，开始生成作品。",
    "loginDesc": "登录以访问您的收藏。",
    "email": "邮箱",
    "emailPlaceholder": "例如：user@example.com",
    "emailHint": "请输入有效的邮箱地址",
    "password": "密码",
    "accessCode": "邀请码（可选）",
    "accessCodePlaceholder": "用于管理员权限",
    "processing": "处理中...",
    "createAccount": "创建账户",
    "haveAccount": "已有账户？",
    "noAccount": "没有账户？",
    "loginInstead": "改为登录",
    "createOne": "创建账户"
  },
  "errors": {
    "authFailed": "认证失败",
    "unexpectedError": "发生意外错误。",
    "invalidEmail": "邮箱格式无效。",
    "wrongCredentials": "邮箱或密码错误，请检查后重试",
    "emailNotConfirmed": "请先确认您的邮箱地址。请检查您的邮箱收件箱（包括垃圾邮件文件夹）",
    "userNotFound": "该邮箱未注册，请先注册",
    "loginFailed": "登录失败，请稍后重试",
    "noUserInfo": "登录失败，未获取到用户信息",
    "passwordTooShort": "密码至少需要 6 个字符",
    "emailExists": "该邮箱已被注册，请使用其他邮箱或直接登录",
    "registerFailed": "注册失败，请稍后重试",
    "noUserCreated": "注册失败，未创建用户",
    "registerSuccessNoProfile": "注册成功，但无法获取用户资料。请稍后重试登录",
    "insufficientCreditsError": "积分不足。需要 {needed} 积分，但您只有 {have} 积分。",
    "apiKeyNotConfigured": "API Key 未配置。请在 .env.local 中设置 GEMINI_API_KEY",
    "apiKeyInvalid": "API Key 无效。请检查 .env.local 中的 GEMINI_API_KEY 配置",
    "networkError": "网络连接失败。请检查网络连接或 API 服务是否可用",
    "apiForbidden": "API 访问被拒绝。请检查 API Key 权限或配额",
    "rateLimited": "API 请求频率过高，请稍后重试",
    "generationFailed": "图片生成失败",
    "deductFailed": "积分扣除失败，请重试"
  }
}
```

- [ ] **Step 2: 创建英文翻译文件**

创建 `i18n/messages/en.json`:

```json
{
  "common": {
    "loading": "Loading...",
    "login": "Login",
    "logout": "Logout",
    "create": "Create",
    "tryDemo": "Try Demo",
    "cancel": "Cancel",
    "save": "Save",
    "download": "Download",
    "search": "Search",
    "clear": "Clear"
  },
  "header": {
    "title": "Nano Banana",
    "pro": "Pro",
    "subtitle": "Gemini 3 Pro Gallery",
    "credits": "Credits",
    "superUser": "Super User",
    "creator": "Creator"
  },
  "hero": {
    "title": "Discover & Create with",
    "titleHighlight": "Nano Banana Pro",
    "description": "Explore a curated collection of Gemini 3 Pro prompts and templates. Generate stunning visuals, remix existing styles, and build your personal library."
  },
  "search": {
    "placeholder": "Search templates by title or prompt..."
  },
  "tabs": {
    "gallery": "Gallery",
    "myLibrary": "My Library",
    "review": "Review",
    "publicStyles": "Public Styles",
    "pendingStyles": "Pending",
    "personalStyles": "Personal Styles"
  },
  "template": {
    "refImage": "Ref Image",
    "refs": "Refs",
    "pending": "Pending",
    "rejected": "Rejected",
    "approve": "Approve",
    "reject": "Reject",
    "tryThis": "Try this",
    "by": "By",
    "anonymous": "Anonymous",
    "copyPrompt": "Copy Prompt"
  },
  "empty": {
    "noTemplates": "No templates found",
    "libraryEmpty": "You haven't created any templates yet. Start creating to build your personal collection.",
    "galleryEmpty": "The public gallery is empty.",
    "createFirst": "Create First Template"
  },
  "loadMore": "Load More",
  "footer": {
    "copyright": "Copyright © 2025, Created by XCC"
  },
  "generator": {
    "title": "Studio",
    "parameters": "Parameters",
    "preview": "Preview",
    "prompt": "Prompt",
    "promptPlaceholder": "Describe your imagination...",
    "referenceImages": "Reference Images",
    "addReferences": "Add References",
    "enabled": "Enabled",
    "addImages": "Add Images",
    "ratio": "Ratio",
    "quality": "Quality",
    "count": "Count",
    "standard": "Standard",
    "high": "High",
    "ultra": "Ultra",
    "availableCredits": "Available Credits",
    "loginToGenerate": "Login to Generate",
    "generating": "Generating...",
    "insufficientCredits": "Insufficient Credits",
    "generate": "Generate",
    "credit": "Credit",
    "credits": "Credits",
    "publishRemix": "Publish Remix",
    "saveTemplate": "Save Template",
    "previewPlaceholder": "Preview will appear here",
    "selected": "SELECTED",
    "editParameters": "Edit Parameters"
  },
  "save": {
    "title": "Save to Gallery",
    "description": "Give your creation a memorable title.",
    "titlePlaceholder": "E.g., Neon Cyber Samurai",
    "publicTemplate": "Public Template",
    "privateDraft": "Private Draft",
    "publicDesc": "Visible to everyone on homepage",
    "privateDesc": "Only visible in your library",
    "saveTemplate": "Save Template"
  },
  "exitConfirm": {
    "title": "Unsaved Changes",
    "description": "You have unsaved work. Are you sure you want to discard it and leave?",
    "keepEditing": "Keep Editing",
    "discardExit": "Discard & Exit"
  },
  "login": {
    "joinStudio": "Join the Studio",
    "welcomeBack": "Welcome Back",
    "joinDesc": "Create your profile to start generating.",
    "loginDesc": "Login to access your library.",
    "email": "Email",
    "emailPlaceholder": "e.g., user@example.com",
    "emailHint": "Please enter a valid email address",
    "password": "Password",
    "accessCode": "Access Code (Optional)",
    "accessCodePlaceholder": "For admin privileges",
    "processing": "Processing...",
    "createAccount": "Create Account",
    "haveAccount": "Already have an account?",
    "noAccount": "Don't have an account?",
    "loginInstead": "Log in instead",
    "createOne": "Create an account"
  },
  "errors": {
    "authFailed": "Authentication failed",
    "unexpectedError": "An unexpected error occurred.",
    "invalidEmail": "Invalid email format.",
    "wrongCredentials": "Invalid email or password. Please check and try again.",
    "emailNotConfirmed": "Please confirm your email address first. Check your inbox (including spam folder).",
    "userNotFound": "This email is not registered. Please sign up first.",
    "loginFailed": "Login failed. Please try again later.",
    "noUserInfo": "Login failed. Could not retrieve user information.",
    "passwordTooShort": "Password must be at least 6 characters.",
    "emailExists": "This email is already registered. Please use a different email or login directly.",
    "registerFailed": "Registration failed. Please try again later.",
    "noUserCreated": "Registration failed. User was not created.",
    "registerSuccessNoProfile": "Registration successful, but could not retrieve profile. Please try logging in later.",
    "insufficientCreditsError": "Insufficient credits. Need {needed} credits, but you have {have}.",
    "apiKeyNotConfigured": "API Key not configured. Please set GEMINI_API_KEY in .env.local",
    "apiKeyInvalid": "Invalid API Key. Please check GEMINI_API_KEY in .env.local",
    "networkError": "Network connection failed. Please check your network or API availability.",
    "apiForbidden": "API access denied. Please check API Key permissions or quota.",
    "rateLimited": "API rate limit exceeded. Please try again later.",
    "generationFailed": "Image generation failed",
    "deductFailed": "Credit deduction failed. Please try again."
  }
}
```

- [ ] **Step 3: 验证 JSON 文件有效**

运行: `node -e "console.log(JSON.parse(require('fs').readFileSync('i18n/messages/zh.json', 'utf8')).common.login)"`
预期: 输出 "登录"

---

## Task 3: 更新中间件和 Next.js 配置

**Files:**
- Modify: `middleware.ts`
- Modify: `next.config.mjs`

- [ ] **Step 1: 更新 middleware.ts**

```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  // 匹配所有路径，除了 api、_next、静态文件等
  matcher: ['/', '/(zh|en)/:path*']
};
```

- [ ] **Step 2: 创建或更新 next.config.mjs**

```javascript
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default withNextIntl(nextConfig);
```

- [ ] **Step 3: 验证配置正确**

运行: `npm run build`
预期: 构建成功

---

## Task 4: 重构路由结构为 [locale] 动态路由

**Files:**
- Create: `app/[locale]/layout.tsx`
- Move: `app/page.tsx` → `app/[locale]/page.tsx`
- Modify: `app/layout.tsx` (简化为最小根布局)

- [ ] **Step 1: 创建新的根布局（简化版）**

更新 `app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Nano Banana Gallery",
  description: "A premium generative art gallery.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
```

- [ ] **Step 2: 创建语言布局**

创建 `app/[locale]/layout.tsx`:

```typescript
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { routing } from '@/i18n/routing';

export default async function LocaleLayout({
  children,
  params: { locale }
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  // 验证 locale 是否有效
  if (!routing.locales.includes(locale as any)) {
    notFound();
  }

  const messages = await getMessages();

  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: 移动首页文件**

```bash
mkdir -p app/[locale]
mv app/page.tsx app/[locale]/page.tsx
```

- [ ] **Step 4: 验证路由工作**

运行: `npm run dev`
访问: `http://localhost:3000` 和 `http://localhost:3000/en`
预期: 页面正常显示，URL 自动重定向到 /zh 或 /en

---

## Task 5: 创建语言切换组件

**Files:**
- Create: `components/LanguageSwitcher.tsx`

- [ ] **Step 1: 创建语言切换组件**

创建 `components/LanguageSwitcher.tsx`:

```typescript
'use client';

import { useLocale } from 'next-intl';
import { useRouter, usePathname } from 'next/navigation';
import { Globe } from 'lucide-react';

const languages = [
  { code: 'zh', name: '中文', flag: '🇨🇳' },
  { code: 'en', name: 'English', flag: '🇺🇸' },
];

export default function LanguageSwitcher() {
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();

  const switchLocale = (newLocale: string) => {
    // 替换路径中的语言前缀
    const segments = pathname.split('/');
    if (segments[1] === 'zh' || segments[1] === 'en') {
      segments[1] = newLocale;
    }
    router.push(segments.join('/'));
  };

  return (
    <div className="relative group">
      <button className="flex items-center gap-1.5 px-3 py-2 rounded-lg bg-neutral-800/50 border border-neutral-700 hover:border-neutral-600 transition-colors">
        <Globe size={14} className="text-neutral-400" />
        <span className="text-xs text-neutral-300">
          {languages.find(l => l.code === locale)?.flag}
        </span>
      </button>
      <div className="absolute right-0 top-full mt-2 w-32 bg-neutral-900 border border-neutral-800 rounded-xl shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all transform origin-top-right z-50">
        {languages.map((lang) => (
          <button
            key={lang.code}
            onClick={() => switchLocale(lang.code)}
            className={`w-full text-left px-4 py-2.5 text-sm hover:bg-neutral-800 first:rounded-t-xl last:rounded-b-xl flex items-center gap-2 transition-colors ${
              locale === lang.code ? 'text-yellow-400' : 'text-neutral-300'
            }`}
          >
            <span>{lang.flag}</span>
            <span>{lang.name}</span>
          </button>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 验证组件正常工作**

运行: `npm run dev`
预期: 组件无报错

---

## Task 6: 更新首页组件使用翻译

**Files:**
- Modify: `app/[locale]/page.tsx`

- [ ] **Step 1: 在首页引入翻译和语言切换器**

在 `app/[locale]/page.tsx` 顶部添加:

```typescript
import { useTranslations } from 'next-intl';
import LanguageSwitcher from '@/components/LanguageSwitcher';
```

在组件内部添加:
```typescript
const t = useTranslations();
```

- [ ] **Step 2: 替换 Header 区域的硬编码文本**

替换以下文本:
- `Login` → `{t('common.login')}`
- `Logout` → `{t('common.logout')}`
- `Create` → `{t('common.create')}`
- `Try Demo` → `{t('common.tryDemo')}`
- `Super User` → `{t('header.superUser')}`
- `Creator` → `{t('header.creator')}`

- [ ] **Step 3: 添加语言切换器到 Header**

在 Header 的右侧区域，Login 按钮之前添加:
```tsx
<LanguageSwitcher />
```

- [ ] **Step 4: 替换 Hero 区域的文本**

```tsx
<h2 className="text-3xl md:text-4xl font-bold mb-4 text-white">
  {t('hero.title')} <span className="text-yellow-400">{t('hero.titleHighlight')}</span>
</h2>
<p className="text-neutral-400 text-lg">
  {t('hero.description')}
</p>
```

- [ ] **Step 5: 替换搜索框占位符**

```tsx
placeholder={t('search.placeholder')}
```

- [ ] **Step 6: 替换 Tabs 区域文本**

```tsx
<button>{t('tabs.gallery')}</button>
<button>{t('tabs.myLibrary')}</button>
<button>{t('tabs.review')} ({pendingCount})</button>
```

以及数量显示:
```tsx
<span>{showMainLoading ? t('common.loading') : `${templates.length} ${activeTab === 'gallery' ? t('tabs.publicStyles') : activeTab === 'review' ? t('tabs.pendingStyles') : t('tabs.personalStyles')}`}</span>
```

- [ ] **Step 7: 替换加载更多和空状态**

```tsx
<button>{isFetching ? <>{t('common.loading')}</> : t('loadMore')}</button>

<h3>{t('empty.noTemplates')}</h3>
<p>{activeTab === 'library' ? t('empty.libraryEmpty') : t('empty.galleryEmpty')}</p>
<button>{t('empty.createFirst')}</button>
```

- [ ] **Step 8: 替换 Footer**

```tsx
<p>{t('footer.copyright')}</p>
```

- [ ] **Step 9: 验证首页翻译正确**

运行: `npm run dev`
访问: `http://localhost:3000/zh` 和 `http://localhost:3000/en`
预期: 语言切换正常，文本正确显示

---

## Task 7: 更新 GeneratorModal 组件

**Files:**
- Modify: `components/GeneratorModal.tsx`

- [ ] **Step 1: 添加翻译导入和 Hook**

```typescript
import { useTranslations } from 'next-intl';

// 在组件内部
const t = useTranslations('generator');
const tCommon = useTranslations('common');
const tSave = useTranslations('save');
const tExit = useTranslations('exitConfirm');
```

- [ ] **Step 2: 替换所有硬编码文本**

按顺序替换:
- `Studio` → `{t('title')}`
- `Parameters` → `{t('parameters')}`
- `Preview` → `{t('preview')}`
- `Prompt` → `{t('prompt')}`
- `Describe your imagination...` → `{t('promptPlaceholder')}`
- `Reference Images` → `{t('referenceImages')}`
- `Add References` / `Enabled` → `{showRefInput ? t('enabled') : t('addReferences')}`
- `Add Images` → `{t('addImages')}`
- `Ratio` → `{t('ratio')}`
- `Quality` → `{t('quality')}`
- `Standard (1K)` → `{t('standard')} (1K)`
- `High (2K)` → `{t('high')} (2K)`
- `Ultra (4K)` → `{t('ultra')} (4K)`
- `Count` → `{t('count')}`
- `Available Credits` → `{t('availableCredits')}`
- `Login to Generate` → `{t('loginToGenerate')}`
- `Generating...` → `{t('generating')}`
- `Insufficient Credits` → `{t('insufficientCredits')}`
- `Generate (X Credit[s])` → `{t('generate')} ({cost} {cost > 1 ? t('credits') : t('credit')})`
- `Publish Remix` → `{t('publishRemix')}`
- `Save Template` → `{t('saveTemplate')}`
- `Preview will appear here` → `{t('previewPlaceholder')}`
- `SELECTED` → `{t('selected')}`
- `Edit Parameters` → `{t('editParameters')}`
- `Unsaved Changes` → `{tExit('title')}`
- `You have unsaved work...` → `{tExit('description')}`
- `Keep Editing` → `{tExit('keepEditing')}`
- `Discard & Exit` → `{tExit('discardExit')}`
- `Save to Gallery` → `{tSave('title')}`
- `Give your creation a memorable title.` → `{tSave('description')}`
- `E.g., Neon Cyber Samurai` → `{tSave('titlePlaceholder')}`
- `Public Template` / `Private Draft` → `{isPublished ? tSave('publicTemplate') : tSave('privateDraft')}`
- `Visible to everyone...` → `{tSave('publicDesc')}`
- `Only visible...` → `{tSave('privateDesc')}`
- `Cancel` → `{tCommon('cancel')}`
- `Save Template` → `{tSave('saveTemplate')}`

- [ ] **Step 3: 验证组件翻译正确**

运行: `npm run dev`
打开生成弹窗，切换语言
预期: 所有文本正确翻译

---

## Task 8: 更新 LoginModal 组件

**Files:**
- Modify: `components/LoginModal.tsx`

- [ ] **Step 1: 添加翻译导入和 Hook**

```typescript
import { useTranslations } from 'next-intl';

// 在组件内部
const t = useTranslations('login');
const tCommon = useTranslations('common');
```

- [ ] **Step 2: 替换所有硬编码文本**

- `Join the Studio` / `Welcome Back` → `{isRegistering ? t('joinStudio') : t('welcomeBack')}`
- `Create your profile...` / `Login to access...` → `{isRegistering ? t('joinDesc') : t('loginDesc')}`
- `Email` → `{t('email')}`
- `e.g., user@example.com` → `{t('emailPlaceholder')}`
- `请输入有效的邮箱地址` → `{t('emailHint')}`
- `Password` → `{t('password')}`
- `Access Code (Optional)` → `{t('accessCode')}`
- `For admin privileges` → `{t('accessCodePlaceholder')}`
- `Processing...` → `{t('processing')}`
- `Create Account` / `Login` → `{isRegistering ? <>{t('createAccount')}</> : <>{tCommon('login')}</>}`
- `Already have an account?` / `Don't have an account?` → `{isRegistering ? t('haveAccount') : t('noAccount')}`
- `Log in instead` / `Create an account` → `{isRegistering ? t('loginInstead') : t('createOne')}`

- [ ] **Step 3: 验证登录弹窗翻译正确**

运行: `npm run dev`
打开登录弹窗，切换语言
预期: 所有文本正确翻译

---

## Task 9: 更新 TemplateCard 组件

**Files:**
- Modify: `components/TemplateCard.tsx`

- [ ] **Step 1: 添加翻译导入和 Hook**

```typescript
import { useTranslations } from 'next-intl';

// 在组件内部
const t = useTranslations('template');
```

- [ ] **Step 2: 替换所有硬编码文本**

- `Ref Image` / `X Refs` → `{template.referenceImages && template.referenceImages.length > 1 ? `${template.referenceImages.length} ${t('refs')}` : t('refImage')}`
- `Pending` → `{t('pending')}`
- `Rejected` → `{t('rejected')}`
- `Approve` → `{t('approve')}`
- `Reject` → `{t('reject')}`
- `Try this` → `{t('tryThis')}`
- `By` → `{t('by')}`
- `Anonymous` → `{t('anonymous')}`
- `Copy Prompt` (title) → `{t('copyPrompt')}`

- [ ] **Step 3: 验证卡片翻译正确**

运行: `npm run dev`
查看模板卡片，切换语言
预期: 所有文本正确翻译

---

## Task 10: 更新 Server Actions 中的错误消息

**Files:**
- Modify: `app/actions.ts`

- [ ] **Step 1: 添加 getLocaleAction 获取当前语言**

```typescript
import { getLocale } from 'next-intl/server';

// 在文件中添加新的 action
export async function getLocaleAction() {
  const locale = await getLocale();
  return locale;
}
```

- [ ] **Step 2: 创建错误消息翻译辅助函数**

```typescript
// 在文件顶部添加
import { getTranslations } from 'next-intl/server';

// 创建获取翻译错误的辅助函数
async function getErrorMessage(key: string, params?: Record<string, string | number>): Promise<string> {
  const t = await getTranslations('errors');
  let message = t(key);
  if (params) {
    Object.entries(params).forEach(([k, v]) => {
      message = message.replace(`{${k}}`, String(v));
    });
  }
  return message;
}
```

- [ ] **Step 3: 替换错误消息返回**

在 `loginAction` 中替换:
- `"Invalid email format."` → `await getErrorMessage('invalidEmail')`
- `"邮箱或密码错误..."` → `await getErrorMessage('wrongCredentials')`
- 等等...

注意：由于 Server Actions 是异步的，这需要大量重构。考虑将错误消息键返回到前端，让前端根据语言显示。

- [ ] **Step 4: 替代方案 - 在前端处理错误翻译**

创建 `components/ErrorMessage.tsx`:

```typescript
'use client';

import { useTranslations } from 'next-intl';

interface ErrorMessageProps {
  errorKey: string;
  params?: Record<string, string | number>;
}

export default function ErrorMessage({ errorKey, params }: ErrorMessageProps) {
  const t = useTranslations('errors');

  let message = t(errorKey);
  if (params) {
    Object.entries(params).forEach(([k, v]) => {
      message = message.replace(`{${k}}`, String(v));
    });
  }

  return <span>{message}</span>;
}
```

然后在 actions.ts 中返回错误键而不是翻译后的消息：

```typescript
return { success: false, errorKey: 'wrongCredentials' };
```

在前端处理：
```typescript
// 在 LoginModal 中
const tErrors = useTranslations('errors');
// ...
setError(result.errorKey ? tErrors(result.errorKey) : result.error);
```

- [ ] **Step 5: 验证错误消息翻译正确**

运行: `npm run dev`
测试登录失败场景
预期: 错误消息根据语言显示

---

## Task 11: 更新 layout metadata 支持多语言

**Files:**
- Modify: `app/[locale]/layout.tsx`

- [ ] **Step 1: 添加动态 metadata 生成**

```typescript
import { getTranslations } from 'next-intl/server';

export async function generateMetadata({
  params: { locale }
}: {
  params: { locale: string };
}) {
  const t = await getTranslations({ locale, namespace: 'header' });

  return {
    title: `${t('title')} ${t('pro')} Gallery`,
    description: locale === 'zh'
      ? 'AI 图像生成画廊应用，基于 Next.js 14 和 Google Gemini API'
      : 'AI Image Generation Gallery powered by Next.js 14 and Google Gemini API',
  };
}
```

- [ ] **Step 2: 验证 metadata 正确**

查看页面源代码
预期: title 和 description 根据语言变化

---

## Task 12: 最终测试和验证

**Files:**
- 无新增文件

- [ ] **Step 1: 运行完整构建**

```bash
npm run build
```

预期: 构建成功，无错误

- [ ] **Step 2: 测试所有页面和功能**

测试清单：
- [ ] 访问 `/zh` 显示中文界面
- [ ] 访问 `/en` 显示英文界面
- [ ] 语言切换器正常工作
- [ ] 首页所有文本正确翻译
- [ ] 登录/注册弹窗文本正确翻译
- [ ] 生成弹窗所有文本正确翻译
- [ ] 模板卡片文本正确翻译
- [ ] 错误消息正确显示
- [ ] 搜索功能正常
- [ ] 加载更多功能正常
- [ ] 移动端响应式正常

- [ ] **Step 3: 提交代码**

```bash
git add .
git commit -m "$(cat <<'EOF'
feat: add internationalization support for Chinese and English

- Add next-intl integration with App Router
- Create language switcher component
- Add translation files for zh and en
- Update all components to use translations
- Support dynamic metadata for each locale
- Add [locale] routing structure

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## 注意事项

1. **SEO 考虑**: 每个语言版本的页面有独立 URL，有利于 SEO
2. **Cookie 存储**: next-intl 默认使用 Cookie 存储语言偏好
3. **服务端翻译**: Server Actions 中的错误消息需要特殊处理
4. **图片文本**: 如果有图片中的文字，需要考虑替换方案
5. **日期/数字格式**: 可使用 next-intl 的格式化功能（如需要）